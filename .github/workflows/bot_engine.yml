name: Daily Satellite Report

on:
  schedule:
    - cron: '0 3 * * *' # Runs at 04:00 AM Polish time (03:00 UTC)
  workflow_dispatch: # Allows manual trigger from GitHub Actions tab

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up Python environment
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install required libraries
        run: |
          python -m pip install --upgrade pip
          pip install requests python-dotenv

      - name: Execute Spy Engine
        run: python spy_engine.py

      - name: Sync results to repository
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git add data.json
          git commit -m "Automated update: $(date)" || echo "No changes detected"
          git push